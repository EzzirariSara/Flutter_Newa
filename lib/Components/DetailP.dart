import 'package:flutter/material.dart';

class Detailp extends StatelessWidget {
  const Detailp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}